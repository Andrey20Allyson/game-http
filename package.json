{
  "name": "game_http",
  "version": "1.0.0",
  "description": "",
  "main": "node dist/index.js",
  "scripts": {
    "build": "tsc -p src && tsc -p client",
    "build:server": "tsc -p src",
    "build:client": "tsc -p client",
    "build-start": "tsc -p src && tsc -p client && node dist/index.js",
    "start": "node dist/index.js",
    "test": "vitest",
    "dev:server": "ts-node-dev src/index.ts --respawn --transpile-only"
  },
  "keywords": [],
  "author": "Andrey Allyson",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",
    "socket.io": "^4.5.1",
    "socket.io-client": "^4.5.1",
    "typescript": "^4.8.3",
    "vitest": "^0.23.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^18.7.16",
    "ts-node-dev": "^2.0.0"
  }
}
